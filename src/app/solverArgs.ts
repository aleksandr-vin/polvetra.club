export interface Args {
  sail_experience_as_captain: string;
  skipper_rating_threshold: number;
  kids_age_threshold: number;
  no_kids_tag: string;
  lambda_weight: number;
  gender_disparity_weight: number;
}
